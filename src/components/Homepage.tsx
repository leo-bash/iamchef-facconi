const Homepage = () => {
  // Title and logo were removed from the homepage to avoid duplication
  // since the Header's TitleSubtitle now renders the same content for the homepage.
  return <div className="w-full h-full flex flex-col items-center justify-start gap-6 pt-10" />
}

export default Homepage
